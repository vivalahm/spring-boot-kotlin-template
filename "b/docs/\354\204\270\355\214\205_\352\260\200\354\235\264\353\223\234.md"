# 프로젝트 세팅 가이드

이 문서는 새로운 개발자가 프로젝트를 빠르게 시작할 수 있도록 기본적인 설정 과정을 설명합니다.

1. **JDK 17 설치**
   - 환경 변수 `JAVA_HOME`이 올바르게 설정되어 있는지 확인합니다.
2. **Gradle Wrapper 사용**
   - 저장소 루트에서 `./gradlew build` 명령을 실행하여 필요한 의존성을 받습니다.
3. **Nexus 주소 변경**
   - 폐쇄망 환경이라면 `gradle.properties` 파일의 `nexusUrl` 값을 사내 Nexus 주소로 수정합니다.
4. **테스트 실행**
   - `./gradlew test` 명령으로 모든 테스트가 통과하는지 확인합니다.

이 단계들을 완료하면 기본적인 개발 환경 구성이 끝납니다.
